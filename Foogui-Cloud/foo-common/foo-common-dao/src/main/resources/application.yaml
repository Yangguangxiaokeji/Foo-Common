spring:
  # 配置数据源位置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3308/foo-modules?characterEncoding=utf8&useSSL=false&serverTimezone=UTC
    username: root
    password: root

